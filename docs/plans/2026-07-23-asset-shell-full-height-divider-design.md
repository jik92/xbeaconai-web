# 资产页共享壳层高度与分割线设计

## 设计

仅修改共享 `AssetPageShell`。页面提供 `sidebar` 时，壳层自动在侧栏与内容区之间显示一条 `border-line` 竖向分割线；无侧栏页面不显示。

壳层高度与 AppShell 的 56px 顶栏一致，使用 `calc(100vh - 56px)` 铺满内容行，避免底部露出灰色画布。各资产页面不增加专用 CSS，业务布局和逻辑保持不变。

## 验证

更新共享组件测试，运行类型检查和生产构建，不运行 E2E。
