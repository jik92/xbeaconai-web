# 素材选择器直接预览设计

目标是在 AI 创作素材选择器中默认展示图片分类，去掉“全部”，并让图片缩略图与视频封面在卡片中直接可见。

设计：当当前模型同时支持图片和视频时，素材库工具栏只显示“图片”和“视频”两个分类，初始为图片；仅支持一种类型时不显示分类切换。素材卡以固定缩略图加文字信息呈现，图片直接加载缩略图，视频直接加载静音封面和时长；原有大小、格式、限制与不可选提示继续保留。

验证：覆盖默认分类、无“全部”选项、图片/视频卡直接使用认证媒体预览，并运行相关单测、类型检查与构建。
