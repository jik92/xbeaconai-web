# Aligned Select Width Design

- Every Select fills the shared form control column.
- Secondary actions must not reduce Select width.
- Place the save-location default action below the Select, aligned right.
- Keep the shared 96px label column and compact spacing.
