# 一键成片默认分镜段数设计

一键成片新建项目的默认分镜段数从 1 调整为 3。AI 填充参数的结构化返回模板同步使用 3，避免用户点击 AI 填充后默认值退回 1。

现有项目保存的分镜段数不迁移、不覆盖；只有新建表单和新的 AI 分析结果采用该默认值。保留现有 1–12 段校验，以及按视频时长保证单段不超过 15 秒的归一化逻辑。

验证覆盖前端初始值和 AI 提示模板，并运行相关单测、类型检查和生产构建。
