# Modal 单行中文标题设计

## 范围

系统内所有 Modal 和抽屉式 Dialog 标题，包括附件选择、素材选择、商品选择、人像选择、项目记录、任务详情和账号工作区抽屉。

## 规则

- 删除标题上方的英文眉题、分类标签和双层标题结构。
- 标题区域只保留一个中文名称。
- 标题使用设计系统的黑色正文色 `text-ink`，不使用强调色。
- 保留原有关闭按钮、弹窗尺寸、内容结构和业务逻辑。

## 验证

运行字体检查、TypeScript 类型检查和生产构建；默认不运行 E2E。
