# Content-sized Select Design

- Shared `NativeSelect` sizes itself from selected content instead of filling its parent.
- Preserve arrow padding and cap width at the available container.
- Allow different Select controls to have different natural widths.
- Keep adjacent secondary actions inline when space allows.
